<template>
  <div id="orderform">
    <div class="main" v-for="(item,index) in content" :key="index">
      <!-- 文本类型 -->
      <div class="row" v-if="item.field.type == 'text'">
        <div class="left">{{ item.text }}</div>
        <div class="right">
          <input type="text" v-model="item.value" placeholder="请输入">
        </div>
      </div>
      <!-- 数字 -->
      <div class="row" v-if="item.field.type == 'number'">
        <div class="left">{{ item.text }}</div>
        <div class="right">
          <input type="number" v-model="item.value" placeholder="请输入">
        </div>
      </div>
      <!-- picker -->
      <div class="row" v-if="item.field.type == 'lpysimplepicker'">
        <div class="left">{{ item.text }}</div>
        <div class="right">
          <allPicker :content="item.field.data" :text="item.field.text" :index="item.field.selected[0]" :title="item.text"></allPicker>
        </div>
      </div>

      <div class="row" v-if="item.field.type == 'lpypicker'">
        <div class="left">{{ item.text }}</div>
        <div class="right">
          <!-- <cityPicker :url="item.field.file" :text="item.field.text" :title="item.text"></cityPicker> -->
          <allPicker :url="item.field.file" :title="item.text"></allPicker>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
import datePicker from '@/components/DatePicker'
import allPicker from '@/components/AllPicker'
// import cityPicker from '@/components/CityPicker'
export default {
  name: 'OrderForm',
  components: {
    datePicker,
    allPicker
    // cityPicker
  },
  props: {
    content: {}
  },
  data () {
    return {}
  },
  mounted () {
  }
}
</script>

<style scoped>
@import '../../assets/css/global.css';
.row {
  padding: 0 var(--edge);
  height: 100px;
  line-height: 100px;
  background: #fff;
  border-bottom: 2PX solid #eee;
  display: flex;
  font-size: var(--m-fs);
  & .left {
    flex: 1;
  }
  & .right {
    flex: 2;
    text-align: right;
  }
  & input {
    text-align: right;
    font-size: var(--m-fs);
    border: none;
  }
  & input:focus {
    outline: none;
  }
}
</style>
