<template>
  <div id="singlepicker">
    <div class="select" @click="showPicker">{{ selectedText[0] }}</div>
    <Picker @select="handleSelect(arguments)" :data="content" :selected-index="selectedIndex"
            ref="singlepicker" :title="name" cancelTxt="取消" confirmTxt="确定"></Picker>
  </div>
</template>

<script>
import Picker from '@/components/Picker'
export default {
  name: 'SinglePicker',
  components: {
    Picker
  },
  props: {
    content: {}, // 所有数据
    text: {}, // 初始显示的值
    name: {}, // picker标题
    selected: {},
    index: {}, // 默认选项的索引
    sign: {}
  },
  data () {
    return {
      selectedText: ['请选择时间'], // 外层显示
      selectedIndex: [1] // 默认年月日
    }
  },
  mounted () {
    this.selectedText = [this.text]
    this.selectedIndex[0] = this.index
  },
  methods: {
    showPicker () {
      this.$refs.singlepicker.show()
    },
    handleSelect (args) {
      this.selectedText = args[2]
      this.$emit('update', [args[0][0], this.sign])
    }
  }
}
</script>

<style scoped>
@import '../assets/css/global.css';
#singlepicker {
  border: 2px solid #e5e5e5;
  border-radius: 10px;
}

.select {
  color: #666;
}
</style>
