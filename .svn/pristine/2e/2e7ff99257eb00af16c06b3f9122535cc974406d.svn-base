import Vue from 'vue'
import Router from 'vue-router'
import Strict from '@/components/strict/Strict'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/strict',
      name: 'Strict',
      component: Strict
    },
    {
      path: '/',
      redirect: '/strict'
    }
  ]
})
